{
  "main": "index.js",
  "name": "worker-service",
  "private": true,
  "version": "0.1.9005",
  "description": "worker-service",
  "author": "HSCIC",
  "dependencies": {
    "express": "3.2.4",
    "fs": "0.0.2",
    "htmlencode": "0.0.4",
    "https": "1.0.0",
    "mustache": "2.1.3",
    "request": "2.61.0",
    "xml2js": "^0.4.12"
  },
  "devDependencies": {
    "mocha": "2.2.4",
    "nconf": "0.7.2",
    "node-mocks-http": "1.4.4",
    "supertest": "1.1.0",
    "unit.js": "2.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "mocha --reporter spec test/*Test.js",
    "watch-test": "mocha --watch --reporter spec test/*Test.js",
    "unit-test": "mocha --reporter spec test/*UnitTest.js",
    "watch-unit-test": "mocha --watch --reporter spec test/*UnitTest.js",
    "integration-test": "mocha --reporter spec test/*IntegrationTest.js",
    "watch-integration-test": "mocha --watch --reporter spec test/*IntegrationTest.js"
  }
}
